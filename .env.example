# Postgres connection string from Neon.tech (production)
# Format: postgres://username:password@host:port/dbname?sslmode=require
DATABASE_URL="postgres://your-username:your-password@your-host:5432/your-database?sslmode=require"

# Local development with Docker
# DATABASE_URL="postgres://postgres:postgres@localhost:5432/vanity"

# Base URL for cover images
NEXT_PUBLIC_SPACES_BASE_URL="https://your-image-host.com"

# Admin authentication credentials (required for the admin interface)
# For better security, ADMIN_PASSWORD should be a bcrypt hash
# You can generate a hash using the auth.hashPassword utility or the generate-hash.js script
# Example: node scripts/generate-hash.js yourSecurePassword
#
# Format of bcrypt hash: $2b$10$randomSaltHere.actualHashValueHere
ADMIN_USERNAME="your_secure_username_here"

# IMPORTANT: For production, use a bcrypt hashed password instead of plaintext
# A plaintext password will work but is less secure and will show a warning in logs
ADMIN_PASSWORD="your_secure_password_here_or_bcrypt_hash"

# API Token for authenticating API requests
# This should be a secure random string, at least 32 characters long
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_TOKEN="your_secure_api_token_here"
